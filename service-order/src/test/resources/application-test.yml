spring:
  datasource:
    url: jdbc:h2:mem:orderdb_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
  kafka:
    listener:
      auto-startup: false
    bootstrap-servers: localhost:9092
